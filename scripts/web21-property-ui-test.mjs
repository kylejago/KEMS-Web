import fs from "node:fs";
import assert from "node:assert/strict";
const read=(path)=>fs.readFileSync(path,"utf8");
const pkg=JSON.parse(read("package.json"));
const index=read("public/index.html"),compare=read("public/strategy-comparison.js"),agile=read("public/web21-agile.js"),live=read("public/web21-live.js"),performance=read("public/performance-page.js"),settings=read("public/settings-page.js"),manifest=read("public/site.webmanifest"),worker=read("public/service-worker.js"),css=read("public/web21.css");
const webNumber=Number.parseInt(pkg.version.match(/-web\.(\d+)$/)?.[1]||"0",10);
assert.ok(webNumber>=21,`Expected Web.21 or later, got ${pkg.version}`);
assert.doesNotMatch(index,/>Products</);assert.doesNotMatch(index,/History &amp; scenarios/);assert.match(index,/\/performance\.html/);assert.match(index,/\/settings\.html/);
for(const marker of ["SOLAR","GRID","HOME","BATTERY","EV","sensor.kems_solar_power","sensor.kems_battery_state_of_charge"])assert.match(live,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")));
for(const marker of ["Live Data","Battery & Solar","Full KEMS","Full KEMS Agile","Winner","Cost comparison","Estimated ROI"])assert.match(compare,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),"i"));
for(const marker of ["data-agile-mode","simulated","Solar generation","Battery SoC","Home usage","EV usage"])assert.match(agile,new RegExp(marker,"i"));
assert.match(performance,/sensor\.kems_actual_roi/);assert.match(performance,/sensor\.kems_actual_system_value_today/);assert.doesNotMatch(performance,/sensor\.kems_roi["']/);assert.doesNotMatch(performance,/\/api\/scenarios/);
for(const marker of ["/api/site","/api/home-assistant/status","/api/system/status","/api/system/action","/api/system/update-policy","/api/remote-access/status","beforeinstallprompt","Check for updates","Install ${esc(latest)}","automatic-updates","update-mode","maintenance-start","maintenance-end","automatic-reboot","maintenance-notices","Save update policy"])assert.match(settings,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")));
assert.doesNotMatch(settings,/\/api\/updates/,"Settings must use the real Pi manager status endpoint, not the nonexistent /api/updates route");
assert.match(settings,/\?refresh=1/,"Check for updates must force a fresh release lookup");
assert.match(settings,/action:\"update\"/,"Install update must call the Pi maintenance action");
assert.match(settings,/method:\"PUT\"/,"Maintenance policy must be saved with PUT");
for(const field of ["automaticUpdates","coordinatedUpdates","maintenanceStart","maintenanceEnd","automaticReboot","notifyMaintenance","channel:\"alpha\""])assert.match(settings,new RegExp(field),`Maintenance policy missing ${field}`);
assert.doesNotMatch(manifest,/products\.html/);assert.match(manifest,/performance\.html/);assert.match(manifest,/settings\.html/);assert.match(worker,/performance\.html/);assert.match(worker,/settings\.html/);assert.match(worker,new RegExp(`kems-alpha7-web${webNumber}-shell-v1`));assert.match(css,/@media\(max-width:620px\)/);assert.match(css,/grid-template-areas:"\. solar \." "grid home battery" "\. ev \."/);assert.match(css,/maintenance-time-grid/);
assert.match(live,/observer\.observe\(app,\{childList:true,subtree:false\}\)/,"Live enhancement observer must watch only top-level app replacement");
assert.doesNotMatch(live,/queueMicrotask\(enhance\)/,"Live enhancement must not recursively queue itself from its own DOM writes");
console.log(`Web.${webNumber} property UI contract passed: property-only navigation, panel flow, four-way comparison, Agile toggle, live ROI, manual update controls, maintenance policy controls, PWA shell and non-recursive live enhancement present.`);
